# pearl_registry/INDEX.md — Unexpected Testable Insights

_Not a REJECT/ARCHIVE/PROMOTE verdict — this is a side-ledger for observations
that surfaced during an experiment but aren't about that experiment's own
claim. Protocol: `rules/falsification-ladder.md` § Pearl Registry._
_`hooks/research_health_loop.py` reads this file and flags entries whose
`next_check` has lapsed._

## Index

| Date | Source Experiment | Observation | Falsifiable Prediction | Impact | Trigger Condition | Next Check | Status |
|------|-------------------|-------------|------------------------|--------|--------------------|-----------|--------|
| 2026-07-28 | 20260728-osa-fl-protocol-vs-standard-analysis | Doubt-Driven Development's mandatory "steelman the opposing view" (Step 2) has no discriminator between steelmanning a plausible-but-contestable counter-argument and steelmanning one that is simply factually false — in a pilot case, it caused an agent applying the protocol to explicitly affirm a security claim ("bounded worst case") that an independent real red-team verdict calls FALSE, while a standard-analysis agent with no steelman obligation rejected the same claim cleanly. | If Step 2 is amended to require a quick soundness check before crediting a counter-argument, a re-run of the same Case 2 prompt (Relaxation Map V1 in `experiments/20260728-.../decision.md`) will score higher on anti-pattern-avoidance without the diagnostic content getting worse. | 6 | V1 re-run executed (see decision.md Relaxation Map) OR the next time DDD Step 2 is invoked on a security-relevant claim where the counter-argument's factual soundness hasn't been separately verified | Re-run done 2026-07-28 (see `followup-v1-v2-rerun.md`) — CONFIRMED (7→11/12, beats Arm A). Rule change landed 2026-07-29: `rules/doubt-driven-development.md` Step 2 now requires the soundness check before crediting a steelmanned counter-argument, cites this pilot's before/after evidence inline, redeployed to `~/.claude/rules/`. n=1 confirmation — treat as validated-but-narrow, watch for a case where this discriminator itself needs revision | landed |
| 2026-07-28 | 20260728-osa-fl-protocol-vs-standard-analysis | Perelman's REPEAT-vs-REJECT threshold (Promotion Rule) can under-reject relative to a real expert verdict specifically when that real verdict's confidence was informed by outside corroboration (e.g. external reviews) the protocol-following agent didn't have access to — the Anti-Overfitting Gate's own caution against premature REJECT produced a REPEAT where the real, better-informed expert issued a clean REJECT. | If Arm B's prompt explicitly asks "would this evidence alone, with no outside corroboration, justify a REJECT this strong?" as a Promotion-Rule sub-question, a re-run of Case 1 (Relaxation Map V2) shifts the verdict from REPEAT toward REJECT without the diagnostic content changing. | 4 | V2 re-run executed (see decision.md Relaxation Map) | Re-run done 2026-07-28 (see `followup-v1-v2-rerun.md`) — prediction REFUTED: verdict stayed REPEAT (5→6/12, still loses to Arm A). Sharper finding underneath: real investigator had external corroboration neither arm had — an information-asymmetry confound this pilot's own estimand pre-registered as a risk, now confirmed concrete for Case 1 | refuted-as-stated, confound identified |
| 2026-07-28 | 20260728-hypothesis-arbiter-taxonomy-pilot | Both baseline and taxonomy-augmented `hypothesis-arbiter` SPAWN output independently found the correct Artifact-class hypothesis on the pilot's test case (pre-registered MCID not met — see `null_results/20260728-hypothesis-arbiter-taxonomy-pilot.md`), but the taxonomy-augmented arm's candidate table explicitly separated Mechanistic/Artifact/Confounder/Boundary into distinct labeled rows, while the baseline arm's H₁/H₂/H₃ blended the same idea-family without an explicit class label distinguishing them — an observed difference in generation STRUCTURE, not generation COVERAGE, that the pilot's endpoint (presence/absence) was never designed to detect. | Re-running the SAME pilot's real case through Этап 2 (KILL-DESIGN) for both arms would show whether the taxonomy-augmented arm's explicit class labels produce more distinct, less-overlapping kill-tests per hypothesis (a Kill-Design-quality endpoint) even though Этап 1 (SPAWN) class-coverage was unaffected. | 4 | Next time `hypothesis-arbiter` is dogfooded on a real multi-hypothesis case where Этап 2 kill-test distinctness can be independently graded | 2026-08-15 → reviewed 2026-08-27 (found overdue by a `/boyko-project-radar` run + this cleanup pass), trigger condition still not met — no qualifying dogfood run has happened since 2026-07-28. Not re-run here (requires an actual `hypothesis-arbiter` session, out of scope for a registry-sync pass). Next check reset to 2026-09-27; if still untriggered by then, downgrade impact or archive rather than silently re-extending again. | pending (overdue, honestly reviewed, not solved) |
| 2026-07-28 | overnight-autonomous-followup (no experiment folder — found while scoping the Negative Control fix `_check_controls` was flagged twice for) | `experiments/_template/controls.md`'s own "Result: [ ] PASS [ ] FAIL" field for Negative Control is genuinely ambiguous, and `tests/test_promotion_gate_guard.py` proves it: `_write_passing_experiment` (line 35) writes Negative Control as `[x] PASS` for "bad input correctly rejected", while `REAL_CONTROLS_MD` (line 160-162) writes the IDENTICAL semantic situation as `[x] FAIL`. `_check_controls` currently accepts either, which is why both fixtures pass — but that means the check cannot distinguish a real ambiguity from a genuine placeholder either. | A fix (either convention pick, migrating both fixtures + the template prose to agree) will make `_check_controls` able to actually verify semantic correctness instead of just "was something checked" — testable by adding a case where Negative Control is marked with the WRONG value under the chosen convention and confirming it now fails condition 2. | 5 | User reviews this entry and picks a convention | Resolved 2026-07-29, user picked option (a) unified PASS/FAIL semantics. `_check_controls` now requires PASS specifically for BOTH controls (was: any checked box); `REAL_CONTROLS_MD` migrated to match `_write_passing_experiment`'s existing convention; `experiments/_template/controls.md`'s Negative Control prose rewritten to state the convention explicitly instead of the ambiguous "if this passes..." wording. 2 new regression tests, 2490 passed / 0 failed | resolved |
| 2026-07-29 | 20260728-hypothesis-arbiter-taxonomy-pilot (side-finding, not that pilot's own claim) | The Oracle Adequacy Gate was integrated into `rules/falsification-ladder.md` Step 2b and `hypothesis-arbiter`'s Этап 4 on 2026-07-28 (`d97b7ee`, deduped against the canonical `docs/oracle-adequacy-gate.md`), but its own usefulness has never been tested: the same-day pilot's endpoint only exercised the SPAWN-stage taxonomy, and the Gate operates later in the cycle. It is currently a structurally-reasonable, entirely unvalidated addition — deliberately NOT recorded in `docs/methodology-outcomes.md`, which takes only components that were actually applied and compared. | Feed a deliberately-inadequate evaluator (e.g. one that fails its own negative control, or one given the generator's full reasoning chain) through a `hypothesis-arbiter` cycle with and without the Gate: with it, the run should be marked `ORACLE_INADEQUATE` and produce no verdict; without it, the same run should produce a confident-looking verdict that the Gate would have blocked. If both arms behave identically, the Gate is ceremony. | 5 | Next time `hypothesis-arbiter` or an FL Full-Ladder experiment is run on a case whose evaluator is non-trivial enough that its adequacy is genuinely in question | 2026-09-01 | pending |
| 2026-08-31 | construct-measurement-gate-v0.3-geoscan-copper-dogfood (fourth real dogfood, object sourced from user's own Obsidian vault: `projects/geoscan-gold/dossier-copper-kazakhstan-2026-08-20.md`, first real test of the just-built Step 2a Reference Standard Audit) | Step 2a's three-model framework (H_TARGET_ERROR / H_REFERENCE_ERROR / H_CONSTRUCT_MISMATCH) correctly explained the copper-specificity null result (TEST1 non-significant across 5 independent relabeling variants -- rules out pure H_REFERENCE_ERROR) but could NOT explain a second, real finding in the same case: TEST2 (general mineralization) lost significance (p 0.009->0.093) after fixing a pseudo-replication error in the resampling method (correlated same-deposit zones treated as independent observations). This is neither a target error, a reference error, nor a construct mismatch -- it is an error in HOW target-reference agreement/significance was computed, a distinct category the current three-model framework doesn't name. | If Step 2a is dogfooded on >=2-3 more real cases involving a statistical significance/agreement test (not just a raw target-vs-reference comparison), a similar inference-method artifact (non-independent samples, multiple comparisons, improper resampling) will recur as a genuine 4th category needing its own name (tentatively: H_INFERENCE_ARTIFACT) alongside the existing three, unless it's judged to already be adequately covered by Step 4's Construct Validity checks or a different existing step. | 6 | Next real dogfood of `construct-measurement-gate` on a case where target-reference agreement is assessed via a significance/resampling test, not just direct comparison | 2026-09-30 | **partially confirmed 2026-08-31, n=2**: seventh real dogfood run (GeoScan Gold Bakyrchik prospectivity model, object sourced from user's own Obsidian vault: `projects/GeoScan Gold — Validation Quick Wins 2026-06-01.md`) found the SAME class of mechanism in the project's own documented (but not-yet-executed) bootstrap-significance plan: 100 IsolationForest reruns with different random seeds on the SAME fixed 199-scene/single-deposit dataset, reported as a 95% CI / p-value on "is 1.13% recall real or luck" -- this treats seed-to-seed model-training variance as if it were sampling variance over the population of gold deposits/regions, the identical non-independence-treated-as-independence structure as the copper case's TEST2 pseudo-replication, just caught prospectively (in a written validation plan, before 2-3 days of compute were spent) rather than retrospectively (via a before/after significance change). Evidentiary note: this confirmation is weaker in one respect than the copper case (a design flaw identified by inspection, not a demonstrated before/after significance flip) but is a genuinely independent second real occurrence, different specific pipeline, same broader project. n=2 of the registered ">=2-3" bar -- one short of the 3-domain standard the sibling Reference-Standard-Audit pattern (row below) was itself held to before building. Not built yet; flagged to user for a call on whether n=2 (with the prospective/retrospective caveat above) suffices or whether to hold for a third, ideally OUTSIDE geoscience entirely (per the same domain-diversity discipline used for the Reference Standard Audit pattern). Verdict for this cell: pending -- do NOT add a 4th H-model yet; n=2, same discipline that gated Step 2a itself (waited for n=3 across 3 domains). **Correction 2026-08-31 (bottleneck-swarm dogfood, primary sources read directly -- Hurlbert 1984 pp.187-196, Nadeau & Bengio 1999/2003):** the n=2 count conflated two DIFFERENT mechanisms as one. Copper (TEST2, correlated same-deposit zones treated as independent) matches Hurlbert-style spatial/design non-independence. Bakyrchik (100 IsolationForest reruns on ONE fixed dataset, no train/test fold structure at all) does NOT match Nadeau-Bengio's mechanism (which requires overlapping CV folds) -- it is a distinct, not-yet-literature-matched mechanism: seed-training-variance reported as if it were population-sampling variance. Honest count is therefore n=1 per mechanism, not n=2 combined. A proposal from the same swarm run to merge this with the ArgosArb reference-independence candidate (row below) into one "Independence Audit" gate was considered and explicitly NOT adopted -- it bundled >=3 mechanistically distinct concerns (Hurlbert-spatial / seed-variance-as-population-variance / reference-independence) under one name, the same category error this correction is fixing. Do not build anything from this row; wait for a real second Bakyrchik-mechanism-specific confirmation before renaming/promoting. |
| 2026-08-31 | construct-measurement-gate-v0.3-argosarb-h8-skeptic-engine-dogfood (eighth real dogfood, object sourced from user's own Obsidian vault: `raw/processed/argosarb_real_world_validation_strategy.md`) | Step 2a's existing 5 reference-standard categories (gold standard / criterion standard / accepted proxy / consensus label / imperfect reference) do NOT cover a reference that fails on INDEPENDENCE rather than accuracy -- here, "Skeptic Engine" was used as the reference standard for ArgosArb's H8 validator, but both systems are the same research effort's own tools, with author-assigned (not externally adjudicated) labels on synthetic cases; the source document's own text admits "Ground truth = researcher judgment (for synthetic cases)". Forcing this into "imperfect reference" would launder an independence failure as an accuracy caveat -- exactly the failure mode this gap risks if left unaddressed. | If Step 2a is dogfooded on >=2-3 more real cases where the named reference standard was built by the SAME research effort as the target system (not an external lab/panel/court/editorial body), the same independence-vs-accuracy gap will recur, warranting a 6th reference-type category (tentatively: "self-referential / non-independent designed benchmark") or an explicit independence-check caveat inserted before Step 2a's existing classification step. | 6 | Next real dogfood of `construct-measurement-gate` on a case with a named reference standard built by the same team/program as the target system | 2026-09-30 | pending -- n=1, do NOT add a 6th category yet, same discipline that gated Step 2a itself (n=3 across domains) and H_INFERENCE_ARTIFACT (n=2 so far) before building |
| 2026-09-04 | 20260903-memory-retrieval-repair (post-PR-349 live re-measurement -- an honest complication, not a claim that the fix backfired) | Excluding the 188-file finding (PR #349: legacy git-feat-/git-fix-* + auto-generated cogniml-skill-* notes) shrank the search corpus further (229 -> 76 files) and, unexpectedly, LOWERED the measured semantic-search delta on the frozen retrieval_v1.jsonl benchmark: Floor 0.156->0.219, Observed 0.344->0.312, Delta 0.188->0.094 (back near the ORIGINAL pre-cleanup delta of 0.094, though both Floor and Observed remain well above the original 0.062/0.156). Root cause, verified concretely: (a) benchmark query q18's OWN ground-truth answer ("2026-04-12_git-feat-15bb5ffd.md", relevant_rel_paths in retrieval_v1.jsonl) was itself one of the excluded legacy git-capture files -- confirmed it now lives under wiki/auto_capture/ -- making q18 structurally unanswerable by this corpus, a benchmark-construction artifact from before this noise category was identified, not a retrieval regression; (b) more importantly, as the corpus got cleaner, Floor (keyword-only) improved almost as much as Observed did, narrowing the gap Delta measures -- plausible mechanism: with less noise for keyword matching to compete against, plain keyword search alone becomes adequate for more queries, so semantic search's marginal value-add (what Delta specifically measures) shrinks even as absolute retrieval quality (Observed itself) stays far above the pre-cleanup baseline. | If the frozen benchmark is regenerated (or q18 is retired/replaced as a known-artifact exception) and re-measured on this same 76-file corpus, Delta should recover closer to the 229-file-corpus figure of +0.188, since removing q18's structural miss and any other now-invalid ground-truth answers would raise Observed without changing Floor much. Separately: if Floor keeps climbing as the corpus is cleaned further (a testable, falsifiable prediction on its own), Delta may keep compressing regardless of q18 -- worth tracking as its own methodology finding about this project's own §5.3 gate, not specific to this benchmark. | 6 -- affects how to interpret ANY future re-measurement of this specific gate as the corpus keeps improving, but doesn't change the substantive conclusion (both fixes measurably improved retrieval) | Next time retrieval_v1.jsonl is re-measured after another corpus change, or the benchmark itself is revised/regenerated | 2026-10-04 | pending -- flagged, not acted on; the frozen benchmark was deliberately NOT modified to preserve its own frozen-before-implementation integrity per falsification-ladder.md, even though q18 is now a known artifact |
| 2026-09-04 | 20260903-memory-retrieval-repair (post-merge live redeploy verification, Pearl Gate — an unexpected but testable finding surfaced while re-verifying PR-5's fix on the real live corpus, not about PR-5's own claim) | Re-measuring the frozen `retrieval_v1.jsonl` benchmark against the CURRENT live `~/.claude/memory/_auto/wiki/` corpus (2061 files, up from ~1940 at freeze time) reproduced the same directional gate result (Floor 2/32=0.062, Observed 5/32=0.156, Δ=+0.094 -- still positive, still qualitatively the same finding as the frozen +0.125) but found the corpus itself has drifted hard: **1756 of 2061 wiki files (85%) are generic one-line `auto_capture.py` commit-capture notes** (tag `#auto-capture`, e.g. "fix: re-verify 027/028/032..."), and 27 of 32 benchmark queries' top-3 candidates are now dominated by these near-duplicate, low-information notes rather than the curated lessons/pattern/research notes the benchmark targets. `auto_capture.py` is a real, deliberately-shipped repo hook (`hooks/registry.yaml:527`) firing on every git commit across every repo on this machine, not a bug -- but its accumulated volume is diluting both TF-IDF term weight (generic engineering vocabulary inflates keyword scores for irrelevant hits) and the dense-embedding neighborhood for the exact retrieval system this session's whole TZ was built to make trustworthy. | If `auto_capture.py`'s notes are excluded from `knowledge_librarian.py`'s candidate pool (or down-weighted, or capped in volume/retention), a re-run of this same live-corpus benchmark measurement will show Observed Hit Rate@3 recover toward or above the original frozen +0.125, without touching PR-5's own scoring code. | 8 -- this isn't a narrow bug, it's the corpus feeding EVERY SessionStart retrieval query on this machine, and it's actively growing (every future commit adds another note) | Next time the live corpus is re-measured against `retrieval_v1.jsonl` (or a fresh benchmark), or the owner decides whether `auto_capture.py` output should be excluded/down-weighted/capped in `knowledge_librarian.py`'s tiering | 2026-10-04 | pending -- flagged to owner, not fixed here (out of this TZ's declared scope: PR-5 repaired retrieval MECHANICS, this is a corpus-CONTENT/auto_capture.py design question); real numbers from `measure_gate_live_retest.py`, not estimated |
| 2026-09-04 | 20260903-memory-retrieval-repair (post-PR-347 live re-measurement -- confirming the auto_capture.py exclusion fix, not that fix's own claim) | Excluding `#auto-capture`-tagged notes (PR #347) shrank the live search corpus from ~2061 to 229 files and raised the frozen `retrieval_v1.jsonl` benchmark's Observed Hit Rate@3 from 5/32=0.156 to 11/32=0.344 (delta 0.094 -> 0.188) -- confirming the fix worked, real numbers, not assumed. But re-measuring also surfaced TWO further, smaller, DIFFERENT noise sources in the remaining 21 misses' wrong top-3 candidates: (1) 83 files named `git-feat-*.md`/`git-fix-*.md` (e.g. `2026-04-12_git-feat-c9c564cd.md`), tagged only `#feat #git` -- an older/different capture mechanism than auto_capture.py, predating or parallel to it, with NO `#auto-capture` marker; (2) 105 files named `cogniml-skill-*.md`, tagged `#cogniml #retrospective #auto-generated #negative-example #fix` -- a CogniML retrospective auto-generator, tagged `#auto-generated`, not `#auto-capture`. Neither is covered by PR #347's exclusion (which only matches the literal `#auto-capture` marker), and both are genuinely out of scope for what was asked (auto_capture.py specifically) -- not fixed here. | If `#auto-generated` and the `git-feat-`/`git-fix-` naming pattern (or a shared marker covering both, e.g. a broader "#low-signal-capture" convention) were also excluded the same way PR #347 excludes `#auto-capture`, a re-run of this same live-corpus benchmark measurement would show a further increase in Observed Hit Rate@3, since these 188 files (83+105) currently still occupy candidate slots in several of the 21 remaining misses. | 5 -- smaller scale than the auto_capture.py finding (188 files vs 1835), same mechanism, easy follow-on if the owner wants it | Next time the owner wants to repeat the auto_capture.py-style exclusion for a different auto-generation source, or the next live-corpus benchmark re-measurement | 2026-10-04 | pending -- flagged, not built; genuinely a different, smaller finding from PR #347's own scope, not silently bundled into it |
| 2026-09-04 | 20260903-memory-retrieval-repair (PR-5, Caveat Gate — a specific named-but-untested alternative from claim.md's own "What this does NOT mean" section) | PR-5 fixed the HOT-tier scoring blend for DENSE (semantic) hits only, after measuring that the pre-existing 14-day-half-life recency-decay formula (`_score_entry`, 70% recency / 30% frequency) structurally suppressed a genuinely strong 58-day-old dense match against a same-day noise match (reproduced concretely in `debug_score_breakdown.py`: 0.293 vs 0.395 final score). The identical 50/50 keyword-overlap/recency blend was left completely unchanged for KEYWORD-sourced hits — `claim.md` explicitly flags this as "a known, separate, out-of-scope design question," not a measured defect for the keyword path. | If the frozen `retrieval_v1.jsonl` benchmark's keyword-only floor measurement is re-run stratified by note age (or a new age-stratified keyword-query slice is added), an old-but-lexically-exact-match note will be measurably outranked by a newer, weaker keyword match at a rate similar to the dense-hit case PR-5 already fixed — i.e. the same category of retrieval miss recurs on the keyword path, just never measured there. | 6 | Next time HOT/WARM/COLD tiering is dogfooded or measured with an age-stratified keyword query set, or a user reports an old note with an exact keyword match getting buried below a newer, weaker match | 2026-10-04 | pending — not built; PR-5's own gate only required fixing the measured dense-hit gap, and per this session's own Anti-Overfitting discipline, do not touch the keyword path without a measured failure driving it (unlike the dense-hit fix, which had a concrete Floor–Ceiling gate failure) |
| 2026-08-31 | construct-measurement-gate-v0.2-sleep-tracker-dogfood (no experiment folder — skill dogfood run in this session, first real use of the diagnostic-only v0.2) | The gate's Measurement Chain (Step 2) placed PSG (polysomnography) at `REAL PHENOMENON`, treating it as ground truth for sleep architecture. User's fact-checked review caught this: PSG is itself an operationalized measurement system (AASM scoring rules applied to EEG/EOG/EMG signals), a reference standard, not the phenomenon itself. Discrete N1/N2/N3/REM labels are themselves a construct choice, not a direct readout of "true sleep architecture." This pattern — conflating a reference/gold standard with ground truth — generalizes far beyond sleep tracking: pathology labels, radiology reads, psychiatric rating scales, human-annotated ML benchmark labels all have the same structure (a "reference" that is itself a fallible measurement system). | If `construct-measurement-gate` is dogfooded on ≥2-3 more real cases with a distinguishable target-vs-reference-standard structure (e.g. an ML classifier vs human-annotated labels, a rapid diagnostic test vs a clinical gold-standard biopsy), the same conflation (reference silently placed at `REAL PHENOMENON` instead of its own measurement branch) will recur unless a dedicated Reference Standard Audit sub-gate exists. Proposed sub-gate text (not yet built): explicitly branch REAL/LATENT PHENOMENON into two parallel operationalized paths (reference standard's own chain, target instrument's own chain), classify the reference as gold/criterion/accepted-proxy/consensus-label/imperfect-reference, and require H_TARGET_ERROR / H_REFERENCE_ERROR / H_CONSTRUCT_MISMATCH as three distinct candidate explanations for any target-reference disagreement, not an automatic "target is wrong." | 8 | Next real dogfood run of `construct-measurement-gate` on a case with a named reference/gold standard | 2026-09-30 | **partially confirmed 2026-08-31, n=2**: second real dogfood run (AI melanoma classification vs dermatopathologist histology consensus) recurred the SAME pattern, more starkly — 8 expert pathologists reached full agreement in only 53.5% of 792 real melanoma-suspicious cases (Nature Communications, 2025), interobserver kappa as low as 0.27 for some histologic features; the "gold standard" AI is validated against is itself a fallible, only-moderately-reproducible visual-interpretation process, structurally identical to the sleep-tracker case's PSG/AASM-rules finding. **Confirmed a third time 2026-08-31, n=3** — deliberately chosen OUTSIDE medicine this time (automated essay scoring vs human raters) to test generalization, not just accumulate same-domain hits. Human-rater reliability itself is only Cohen's kappa 0.582-0.709 (moderate, not perfect), the same reference-is-itself-a-measurement-system structure, even though this domain has an unusually explicit industry psychometric bar (QWK >=0.70) most of the other domains lack. Distinct from this pattern (kept analytically separate per Quality Rule 12, not conflated): the SAME case also surfaced a real, adversarially-confirmed construct-contamination failure (Perelman 2014: e-rater scores grammatical-but-meaningless BABEL-generated gibberish 5-6/6) — this is a target-branch gaming vulnerability, not a reference-branch reliability problem, and it maps to an H_MEASURE mechanism ('construct contamination') already named in Step 3 of the existing gate, so it required no new capability, just correct non-conflation. **n=3 now meets the registered >=2-3 threshold across 3 different domains (sleep medicine, dermatopathology, education/psychometrics) — building the Reference Standard Audit sub-gate is now warranted per this entry's own stated criteria.** User confirmed: build it. Landed 2026-08-31 as `construct-measurement-gate` v0.3's Step 2a (Reference Standard Audit) -- see `skills/extensions/construct-measurement-gate/SKILL.md` and `skills/registry.yaml`. Step 2a itself is `described`, not yet independently dogfooded -- the next real run of the gate on a named-reference-standard case is the first test of the built sub-gate, not just the pattern that motivated it. Status: resolved -- built into v0.3. |
