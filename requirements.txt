# Runtime — hooks используют только stdlib (json, re, sys, os, pathlib)
# Внешних зависимостей нет — намеренно: hooks должны запускаться
# без pip install в любом окружении Claude Code.

# Dev / CI
# WHY exact pins (not >=): CI previously ran `pip install pytest pytest-cov ruff mypy`
# directly, ignoring this file entirely and floating to whatever latest each
# release resolved to at run time — the >= floors here were dead documentation,
# not an enforced contract. Exact-pinned so a tool's new major release can't
# silently change lint/type-check behavior and break CI unrelated to any code
# change. Versions below verified clean against this exact tree (2026-07-01):
# pytest -q (1687 passed), ruff check . (clean), mypy hooks/ scripts/ (clean).
pytest==8.4.2
pytest-cov==5.0.0
ruff==0.15.13
mypy==1.19.1
# WHY PyYAML is here (2026-07-16): several structural gates read skills/registry.yaml
# (test_registry_matches_disk, TestDependencyBacklinks, TestRegistryCapabilitySchema).
# Without it in CI's deps they importorskip and pass vacuously -- "registered but not
# enforced", the exact hole the Substrate Gate names. It is a DEV/CI dependency only;
# runtime hooks remain stdlib-only by design (see top of this file).
PyYAML==6.0.2
# WHY RestrictedPython is here (P1, external audit 2026-07-24): hooks/expert_registry.py's
# sandboxed exec path (_compile_expert_code/_execute) uses RestrictedPython to block
# dunder-escape at compile time when running LLM-generated "expert" code -- a real
# security boundary, not experimental. Without it pinned in CI, tests/test_expert_registry.py's
# entire suite (pytest.mark.skipif(not RP_AVAILABLE)) importorskips -- 12 tests exercising
# the actual sandboxed-execution path never ran, only the deliberate absence-fallback path
# in test_expert_registry_sandbox_refusal.py did. Same DEV/CI-only status as PyYAML: hooks/
# stays stdlib-only at runtime (expert_registry.py refuses to run unsandboxed rather than
# degrading when RestrictedPython is absent in production -- see its own F-08 WHY comment).
RestrictedPython==8.4
