# Cursor Harness — Claude-cod-top-2026

See `.cursor/rules/` and run `scripts/install-cursor-harness.ps1` to refresh.

## Cursor > Claude Code
| Feature | Use |
|---------|-----|
| Rules .mdc | Always-on methodology |
| Task subagents | Parallel work |
| MCP | Semgrep, Neo4j, gh via GitKraken |
| Automations | Scheduled agents |
| Loop | Recurring checks |
| Canvas | Rich audits |
| Multitask | Multiple agents |

## Claude Code > Cursor
| Feature | Use |
|---------|-----|
| 60 hooks | install.sh on this repo |
| keyword_router auto | power: prefix in Cursor |
| pattern_extractor auto | learning-loop rule + ritual |

Utilization target: **70-75%** of combined system in Cursor after rules + hook profile split.
