---
name: boyko-scientific-consortium
sub_type: guide
version: "1.2"
status: draft
confidence: >
  dogfooded across 2 sessions (2026-08-09 real infra bug found+fixed via Шаг 2;
  2026-08-10 second session ran 3 hypotheses, self-scored 7/10 again). Same score
  twice is NOT stagnation: v1.1's 2 fixes (Шаг4/5 either-or, min-2-modes) both held up
  cleanly in the second run — the flat score reflects 2 NEW gaps of the SAME underlying
  class (soft "use/choose/check" instructions with no structural gate silently get
  skipped under load; hard gates — tables, required lines, minimum counts — hold).
  v1.2 (2026-08-10) converts the 4 remaining soft spots found in round 2 (historical
  context, depth-tier justification, EstimandOps L0 tier-scope, hypothesis-lab opt-in)
  to the same gate pattern. Still n=2 sessions — treat as dogfooded-but-narrow.
description: >
  Виртуальный междисциплинарный научный консорциум для формулировки, разбора,
  проверки, опровержения и развития гипотез в любой области. Адаптивно подбирает
  роли под задачу (предметный специалист, историк науки, методолог, статистик/
  байесианец, эксперт по причинному выводу, экспериментатор, инженер измерений,
  специалист по моделированию, философ науки, эксперт по воспроизводимости,
  red-team скептик, эксперт по данным) вместо фиксированного набора. Строит явную
  причинную карту (конфаундеры/медиаторы/коллайдеры/обратная причинность/селекция/
  feedback loops), реконструирует тезис до формальной проверяемой версии, прогоняет
  anti-confirmation чек-лист смещений (p-hacking, HARKing, survivorship/publication
  bias, collider bias, Simpson's paradox, Goodharts's law и др.), и добавляет 4 режима,
  которых нет больше нигде в каталоге: Аномалия, Инверсия, Предсказания-до-данных,
  Контрфактуальный мир (+ Уровни масштаба, Решающий эксперимент). Не реализует заново
  то, что уже есть — делегирует EstimandOps L0 в estimand-ops(rule), конкуренцию
  гипотез в hypothesis-arbiter, FALSIFY/BUILD в sci-evidence. Три глубины разбора:
  Быстрый / Глубокий / Максимальный (= FL Micro/Standard/Full).
  Триггеры: /boyko-scientific-consortium, /boyko-consortium, научный консорциум,
  virtual scientific consortium, разбери гипотезу консорциумом, causal map гипотезы,
  причинная карта, anti-confirmation анализ, режим аномалии, режим инверсии,
  контрфактуальный мир, decisive experiment package, research package.
tokens: ~5100  # measured: 36971 bytes / 408 lines in SKILL.md v1.2, 2026-08-10
effort: high
triggers: [/boyko-scientific-consortium, /boyko-consortium, "научный консорциум", "virtual scientific consortium", "разбери гипотезу консорциумом", "причинная карта гипотезы", "anti-confirmation анализ", "режим аномалии", "режим инверсии", "контрфактуальный мир", "decisive experiment package"]
---

<!-- BSV — Brief Skill View | поиск: BSV
Скил   : boyko-scientific-consortium
TL;DR  : Адаптивная команда ролей + причинная карта + anti-confirmation red-team + 4 новых режима (Аномалия/Инверсия/Предсказания-до-данных/Контрфактуальный мир). Делегирует, не дублирует: L0→estimand-ops, конкуренция гипотез→hypothesis-arbiter, FALSIFY/BUILD→sci-evidence.
Вызов  : /boyko-scientific-consortium, /boyko-consortium, "разбери гипотезу консорциумом", "причинная карта"
НЕ для : одиночной фальсификации одной идеи (→ /skeptic), FALSIFY/BUILD одной гипотезы без причинной карты (→ /sci-evidence), фиксированной 9-ролевой панели (→ /hypothesis-lab), генерального 20-линзового разбора не-гипотезы (→ /multi-lens)
-->

# Boyko Scientific Consortium

## Происхождение и зачем этот скилл существует

Это НЕ новый метод — это компоновка. При проверке (2026-08-04, эта же сессия) выяснилось,
что 70-80% функциональности уже покрыто `sci-evidence` + `hypothesis-arbiter` +
`hypothesis-lab` + `multi-lens` + `~/.claude/rules/estimand-ops.md`. Настоящий пробел —
только: (1) **адаптивный**, а не фиксированный подбор ролей, (2) явная **причинная карта**
как отдельный артефакт для любого класса вопроса (не только causal), (3) четыре
именованных режима, которых нет нигде: Аномалия, Инверсия, Предсказания-до-данных,
Контрфактуальный мир. Этот скилл закрывает именно эти три пробела и **делегирует всё
остальное**, а не переизобретает. См. `docs/skill-disambiguation.md` — семья «консорциум».

## Философия

> Наука прогрессирует через систематическое исключение ложного, а не накопление
> подтверждений. Скрытые причинные связи опаснее явных ошибок — они не спорят с тобой,
> они просто отсутствуют в модели, пока их не начнёшь искать целенаправленно.

---

## Шаг 0 — EstimandOps L0 Gate (ОБЯЗАТЕЛЬНО, делегируется)

Не реализуется здесь заново. Прогони классификацию по `~/.claude/rules/estimand-ops.md`:
Descriptive / Predictive / Causal — **до** любого дальнейшего шага. Для Causal — DAG
из Шага 3 этого скилла удовлетворяет требованию estimand-ops «DAG + 4 условия
идентификации» (Шаг 3 ниже — это и есть тот DAG, отдельно строить не нужно).

Если тезис расплывчат — не переспрашивай, сформулируй наиболее сильную проверяемую
версию сам и продолжай (Шаг 1 ниже формализует это явно).

**Объём L0 по тиру (добавлено v1.2, 2026-08-10 — второй дожинающий прогон нашёл, что
граница между "сокращение по тиру" и "пропуск ради скорости" не была явной):**

| Тир | Что считается ПОЛНЫМ L0 (не сокращением) |
|---|---|
| Быстрый / Глубокий | Тип вопроса (Descriptive/Predictive/Causal) + одно предложение-эстиманд + минимум 2 пункта "что НЕ значит" — это вся форма, не урезанная версия чего-то большего |
| Максимальный | Плюс полный `estimand.md`: population/intervention/comparator/endpoint/MCID/ICE, для Causal — DAG (= Шаг 3) + 4 условия идентификации |

---

## Шаг 1 — Реконструкция тезиса

Прежде чем анализировать — разложи явно, в виде таблицы:

| Поле | Значение |
|---|---|
| Точная формулировка (сильнейшая проверяемая версия) | |
| Переменные | входные / выходные / скрытые |
| Предполагаемый механизм | |
| Уровень объяснения | микро / мезо / макро (см. режим «Уровни масштаба») |
| Границы применимости | где заведомо не работает |
| Наблюдение vs интерпретация | что реально измерено, что достроено рассуждением |

---

## Шаг 2 — Адаптивный подбор ролей консорциума

**Отличие от `/hypothesis-lab`:** там всегда 9 фиксированных специалистов. Здесь роли
подбираются ПОД задачу — минимум 3, без верхнего предела, но не декоративно (роль
включается только если у неё есть *реальный* вопрос к этому конкретному тезису).

Ростер ролей (не обязателен полностью, выбирай релевантные + добавляй свои при нужде):

```
предметный специалист · историк науки · методолог · статистик/байесианец ·
эксперт по причинному выводу · экспериментатор · инженер измерений ·
специалист по моделированию/симуляциям · философ науки ·
эксперт по воспроизводимости · red-team скептик · эксперт по данным/computational methods
```

Для каждой выбранной роли: **какой конкретный вопрос к тезису она задаёт, которого
без неё не возникло бы**. Если роль не добавляет уникального вопроса — не включай её,
это не театр, а инструмент.

Показывай разногласия между ролями только когда они вскрывают реальную научную
развилку (не разыгрывай спор ради формы).

---

## Шаг 3 — Причинная карта

Обязательный артефакт для ЛЮБОГО класса вопроса (не только Causal из Шага 0):

| Элемент | Вопрос |
|---|---|
| Наблюдаемые переменные | что измерено напрямую |
| Скрытые переменные | что не измерено, но логически должно влиять |
| Конфаундеры | общая причина X и Y |
| Медиаторы | через что X влияет на Y |
| Коллайдеры | переменная, которую нельзя обусловливать без искажения |
| Обратная причинность | может ли Y вызывать X |
| Временные лаги | задержка эффекта, которую легко принять за отсутствие эффекта |
| Селекционные эффекты | как отобрана выборка/данные |
| Feedback loops | усиливающие/уравновешивающие петли |
| Нелинейности, пороги | где линейная интуиция ломается |
| Эффекты масштаба/сети | меняется ли механизм при росте N |

**Невидимые линии** — отдельно выпиши: что пропущено, какие предпосылки неявны,
и (важно) — может ли **отсутствие** наблюдаемого эффекта само быть информацией
(а не просто "нет данных").

---

## Шаг 4 vs Шаг 5 — выбери ОДИН, не оба (исправлено v1.1, 2026-08-09)

**Известное ограничение первого дожинающего прогона:** таблица «Глубина разбора» ниже
раньше читалась как «на Глубоком разборе — Шаг 4 И Шаг 5 всегда вместе». В реальном
прогоне (см. `confidence` выше) это оказалось неверно и было тихо пропущено без явного
критерия выбора. Правило теперь явное:

| Форма тезиса | Делегируй | Не вызывай |
|---|---|---|
| ≥2 конкурирующих механизма объясняют один и тот же результат | Шаг 4 (`/hypothesis-arbiter`) | Шаг 5 — kill-тесты арбитра уже играют роль mini-FALSIFY |
| Один механизм, нужна доказательная база или его нужно сломать | Шаг 5 (`/sci-evidence`) | Шаг 4 — нечего арбитрировать |
| Оба условия одновременно (редко — ≥2 механизма, и каждый ещё нужно доказывать по отдельности) | Оба, последовательно: Шаг 4 сначала (сузить до выживших), Шаг 5 — только для финального выжившего | — |

### Шаг 4 — Конкуренция гипотез (делегируется)

Если возможных механизмов ≥2 — **не переизобретай**, вызови `/hypothesis-arbiter`
(SPAWN→KILL-DESIGN→IN-SILICO→RED-TEAM→ARBITRATE, включая «Решающий эксперимент» —
этот режим уже там, полностью). Передай ему причинную карту из Шага 3 как контекст.

Если механизм один и конкурентов нет по существу — не вызывай, это ложная генерация
ради формы (`/hyparb`'s own routing table называет это явно: "только один механически
возможный путь" → не его случай).

### Шаг 5 — Фальсификация / доказательство (делегируется)

Вызови `/sci-evidence` (FALSIFY «5 миров» → BUILD «5 путей», Consilience Score).
Не дублируй его протокол здесь — этот скилл добавляет причинную карту и роли
консорциума ДО него и Специальные режимы ниже ПОСЛЕ, сам FALSIFY/BUILD не переписывает.

---

## Шаг 6 — Anti-confirmation чек-лист (то, чего нет полностью нигде в каталоге)

По умолчанию режим — **против** тезиса пользователя: ищи условия, при которых он
должен считаться ослабленным, а не подтверждения. Прогони явно, отмечая наличие/
отсутствие для каждого:

```
cherry-picking · p-hacking · HARKing · survivorship bias · publication bias ·
measurement error · regression to the mean · selection bias · data leakage ·
overfitting · collider bias · Simpson's paradox · Goodhart's law ·
множественные сравнения · нестационарность · внешняя валидность
```

Для каждого найденного риска: конкретный механизм, как именно ОН исказил бы вывод
в ЭТОМ случае (не общий список угроз — точечная привязка к тезису).

---

## Специальные режимы

Первые два — уже есть в каталоге, вызывай их оттуда, не переписывай:
- **Решающий эксперимент** → `/hypothesis-arbiter` Этап 5 (если гипотез ≥2)
- **FALSIFY «5 миров»** → `/sci-evidence falsify` (если гипотеза одна)

Четыре ниже — реализованы **только здесь**, это ядро добавленной ценности скилла.
**Правило выбора (добавлено v1.1, 2026-08-09):** это то, ради чего скилл вообще
существует — не пропускай их молча. На Глубоком разборе — **минимум 2 из 4**, не
только тот, что первым пришёл в голову (первый дожинающий прогон использовал
только Аномалию, пропустив остальные 3 без объяснения). Критерий, какие два:

| Режим | Бери, когда |
|---|---|
| Аномалия | Почти всегда дёшево и стоит того — что модель объясняет хуже всего |
| Инверсия | Тезис — чёткое бинарное утверждение (верно/неверно), не диапазон |
| Предсказания до данных | Есть ЛЮБАЯ возможность реально проверить что-то ДО того, как увидишь результат (эмпирический тест, замер, поиск источника) |
| Контрфактуальный мир | В причинной карте Шага 3 есть конкретный элемент (confounder/mediator), альтернативное значение которого меняет вывод |
| Уровни масштаба | Явление правдоподобно ведёт себя по-разному на разных N/масштабах (сетевые эффекты, эффекты выборки) |

Если тезис не даёт зацепки ни для одного критерия кроме Аномалии — это сигнал, что
задача, возможно, проще, чем кажется, и `/sci-evidence`/`/skeptic` дешевле (см.
Anti-patterns ниже).

### Аномалия
Какие факты текущая модель объясняет ХУЖЕ ВСЕГО? Не «что подтверждает», а «что
торчит и не влезает». Список аномалий важнее списка подтверждений.

### Инверсия
Что ДОЛЖНО быть верно, если исходная гипотеза НЕВЕРНА? Сформулируй позитивные
предсказания альтернативного мира — не просто «гипотеза ложна», а конкретные
наблюдаемые следствия её ложности.

### Предсказания до данных
Прежде чем смотреть результат — запиши, что гипотеза предсказывает. Если пишешь
предсказание ПОСЛЕ того как увидел результат — это не предсказание, а рационализация
задним числом (тот же принцип, что outcome map в `/hyparb`, но применяется здесь
на уровне всего тезиса, а не отдельного kill-теста).

### Контрфактуальный мир
Сравни: что наблюдалось бы, если бы работал ДРУГОЙ механизм (не конкурирующая
гипотеза целиком, а именно альтернативный causal-путь из карты Шага 3)? Чем это
отличается от Шага 4 (конкуренция гипотез)? Там — целые гипотезы. Здесь — один
элемент причинной карты меняется, остальное фиксировано (mini counterfactual, не
полная альтернативная теория).

### Уровни масштаба
Сохраняется ли объяснение на микро-, мезо- и макроуровне? Явно проверь все три,
даже если тезис сформулирован только на одном — расхождение между уровнями само
по себе диагностическая находка.

---

## Исторический контекст

**Обязательный формат (добавлено v1.2, 2026-08-10 — второй дожинающий прогон нашёл,
что без явной таблицы эта секция тихо подменяется внутрипроектной аналогией вместо
реального литературного поиска):**

| Прецедент | Источник (WebSearch query/URL ИЛИ `SOURCE_NOT_FOUND`) | Внешний или внутрипроектный |
|---|---|---|

**Жёсткое правило:** внутрипроектная аналогия (свой же код, свой же скилл, прошлый
разбор в этой же кодовой базе) **не считается** историческим прецедентом — это
Контрфактуальный мир или причинная карта, не история науки. Строка «внешний» в
таблице засчитывается только если реально выполнен WebSearch (или явно написан
`SOURCE_NOT_FOUND`, если поиск дал пусто) — предпочитай первичные статьи и
мета-анализы, различай препринт и рецензируемую публикацию, отмечай размер
выборки/качество дизайна/конфликты интересов. Не выдумывай ссылки.

---

## Эпистемическая маркировка

Переиспользует существующую систему CLAUDE.md Evidence Policy, а не вводит
параллельную: `[VERIFIED]` / `[DOCS]` / `[CODE]` / `[INFERRED]` (указать цепочку) /
`[WEAK]` / `[CONFLICTING]` (обе стороны) / `[UNKNOWN]`. Причина не заводить второй
набор меток («установлено/сильное свидетельство/спекуляция/...») — два конкурирующих
словаря evidence-маркировки в одном репозитории хуже одного последовательного.

Для числовых выводов — допущения, неопределённость, чувствительность к параметрам,
базовые частоты явно. Отсутствие данных ≠ отсутствие эффекта — пиши это прямо, когда
применимо.

---

## Глубина разбора

Не изобретает четвёртую шкалу строгости — маппится на существующую Falsification
Ladder:

| Запрос пользователя | = FL tier | Что входит |
|---|---|---|
| **Быстрый разбор** | Micro | Шаг 1 (тезис) + Шаг 6 (anti-confirmation, коротко) + главные уязвимости |
| **Глубокий разбор** | Standard | + Шаг 2 (роли) + Шаг 3 (причинная карта) + Шаг 4 ИЛИ Шаг 5 (не оба — см. таблицу выбора в разделе «Шаг 4 vs Шаг 5») + **минимум 2 из 4** спецрежимов (см. таблицу критериев в «Специальные режимы») |
| **Максимальный разбор** | Full | Всё + `/hypothesis-lab` (решение обязано быть явным — см. ниже) + историч. контекст + **все 4** спецрежима + Шаг 4 И Шаг 5 если оба применимы + репликация/повторное подтверждение |

**`/hypothesis-lab` на Максимальном (добавлено v1.2, 2026-08-10 — второй дожинающий
прогон нашёл, что этот пункт молча дефолтится в "нет" без обоснования):**
`hypothesis-lab: вызван / не вызван — потому что [причина]` — обязательная строка,
даже когда ответ "не вызван".

**Обязательная первая строка ответа (добавлено v1.2, 2026-08-10 — второй дожинающий
прогон нашёл, что выбор глубины принимается молча, до начала разбора, без показанного
рассуждения):**

```
Глубина: [Быстрый/Глубокий/Максимальный] — потому что [конкретная причина: число
конкурирующих механизмов, есть ли дешёвый решающий тест, цена ошибки].
```

Если пользователь не указал — выбирай по сложности тезиса, не перегружай простой
вопрос Максимальным разбором, но само это решение всё равно обязано появиться в
строке выше, не остаться внутренним.

---

## Формат вывода — исследовательский пакет

```
## 1. Тезис (Шаг 1)
## 2. Причинная карта (Шаг 3)
## 3. Исторические аналоги
## 4. Конкурирующие модели (делегировано /hypothesis-arbiter, если применимо)
## 5. Скрытые допущения
## 6. Критические тесты / Решающий эксперимент
## 7. Ожидаемые результаты при разных моделях
## 8. Возможные артефакты и смещения (Шаг 6 чек-лист)
## 9. План обновления убеждений
## 10. Следующий лучший эксперимент/наблюдение — ОДИН, не список
```

---

## Disambiguation — семья «консорциум/гипотезы» (обновлено в docs/skill-disambiguation.md)

| Скилл | Механизм | Когда брать вместо этого |
|---|---|---|
| `sci-evidence` | FALSIFY/BUILD одной гипотезы, без причинной карты и ролей | Гипотеза одна, причинная структура не нужна, нужна скорость |
| `hypothesis-arbiter` | Конкуренция ≥2 гипотез, kill-тесты | Уже есть чёткий список гипотез-конкурентов, причинная карта не нужна |
| `hypothesis-lab` | ФИКСИРОВАННЫЕ 9 специалистов, Lab Consensus Score | Нужна конкретно эта панель/скоринг, не адаптивный подбор |
| `multi-lens` | 20 универсальных линз для ЛЮБОГО объекта (не только гипотезы) | Объект — не гипотеза (архитектура, код, стратегия) |
| `skeptic` | Точечная одноразовая атака одной идеи | Не нужна причинная карта/роли/спецрежимы, нужна just атака |

**Правило:** если тезис — гипотеза, нужна причинная карта, и/или нужны Аномалия/
Инверсия/Предсказания-до-данных/Контрфактуальный мир — этот скилл. Если нет ни
одного из этих трёх — один из скиллов выше дешевле и быстрее.

---

## Anti-patterns

| Сигнал | Диагноз | Действие |
|---|---|---|
| Все роли Шага 2 включены "на всякий случай" | Театр, не инструмент | Оставь только роли с реальным уникальным вопросом |
| Причинная карта построена, но не используется в Шаге 6 | Артефакт ради артефакта | Каждый confounder карты обязан появиться в anti-confirmation проверке |
| Предсказание в режиме "Предсказания до данных" написано после просмотра результата | Post-hoc rationalization | Отмени как предсказание, повтори ДО |
| Максимальный разбор для простого вопроса | Перегрузка | Понизь до Быстрого/Глубокого по сложности тезиса |
| Дублирование протокола sci-evidence/hypothesis-arbiter внутри этого скилла | Нарушение Wheels-First, риск рассинхронизации | Вызови их, не копируй их шаги сюда |
| И Шаг 4, И Шаг 5 вызваны на Глубоком разборе без причины (по инерции, не потому что оба нужны) | Церемония ради церемонии | Используй таблицу выбора в «Шаг 4 vs Шаг 5» — обычно один, не оба |
| Использован только 1 из 4 спецрежимов на Глубоком разборе (обычно Аномалия — самый очевидный) | Флагманская ценность скилла не раскрыта | Минимум 2 — см. таблицу критериев в «Специальные режимы»; если и правда подходит только 1 — это сигнал, что задача проще, `/sci-evidence` дешевле |
| Секция «Исторические аналоги» заполнена внутрипроектной аналогией (свой же код/скилл) без реального WebSearch | Формальное, не фактическое выполнение — литературный поиск подменён удобной внутренней ссылкой | Обязательная таблица прецедентов с источником; внутрипроектное не засчитывается, только внешний источник или `SOURCE_NOT_FOUND` |
| Глубина разбора выбрана молча, до начала анализа, без показанной причины | Решение принято, но не предъявлено — пользователь не может его оспорить | Обязательная первая строка `Глубина: X — потому что Y` |
| EstimandOps L0 на Быстром/Глубоком выполнен как будто это урезание полного `estimand.md` (с извинениями/пропусками) | Смешаны "полная форма для тира" и "пропуск ради скорости" | Краткая L0 — это ПОЛНАЯ форма для Быстрого/Глубокого, не сокращение; полный estimand.md только на Максимальном |
| На Максимальном `/hypothesis-lab` просто не упомянут, решение не вызвать его не объяснено | Молчаливый дефолт в "нет" | Обязательная строка `hypothesis-lab: вызван/не вызван — потому что...`, даже когда ответ "нет" |

---

## Связанные скилы

- `/sci-evidence` — делегат для FALSIFY/BUILD одной гипотезы
- `/hypothesis-arbiter` — делегат для конкуренции гипотез + Решающий эксперимент
- `/hypothesis-lab` — альтернатива с фиксированной 9-ролевой панелью (для Максимального разбора — дополняет, не заменяет)
- `/multi-lens` — источник паттерна «фиксированный каталог» (здесь — обратный принцип, адаптивный)
- `/skeptic` — независимый red-team для Step 8a (FL), контекстно-слепой
- `~/.claude/rules/estimand-ops.md` — L0 gate, DAG-требования для Causal
- `~/.claude/rules/falsification-ladder.md` — глубина Micro/Standard/Full, на которую маппится Быстрый/Глубокий/Максимальный
- `/construct-measurement-gate` — вызывает как один из downstream adjudication layers для Step 10 (финальная интерпретация measurement-диагностики)

---

## История версий

**v1.2 (2026-08-10)** — второй дожинающий прогон (3 гипотезы в одной сессии, вне
этого репозитория). Оценка 7/10 — та же, что v1.1, и это НЕ стагнация: оба фикса
v1.1 (таблица выбора Шаг4/5, минимум-2-режима) подтвердились как рабочие в этом
прогоне ни разу не нарушены. Плоский счёт объясняется 2 НОВЫМИ пробелами того же
класса — мягкая инструкция без структурного gate тихо режется под нагрузкой,
жёсткий gate (таблица/обязательная строка/минимум-N) соблюдается. Найдено и
исправлено 4 мягких места: (1) исторический контекст заполнялся внутрипроектной
аналогией вместо WebSearch — теперь обязательная таблица прецедентов с источником;
(2) глубина разбора выбиралась молча — теперь обязательная первая строка с причиной;
(3) граница EstimandOps L0 "сокращение по тиру" vs "пропуск ради скорости" не была
явной — теперь explicit таблица объёма по тиру; (4) `/hypothesis-lab` на Максимальном
молча дефолтился в "нет" — теперь обязательная строка решения. Симметрично добавлены
4 строки в Anti-patterns. `confidence` обновлён честно: n=2 сессии, всё ещё
dogfooded-but-narrow, `status` остаётся `draft`.

**v1.1 (2026-08-09)** — первый реальный прогон (гипотеза о зависании SessionStart-хука,
завершился реальным найденным и исправленным багом, commit `b820540`). Оценка 7/10 —
сильная сторона (Шаг 2, адаптивные роли) подтвердилась на деле; две слабости найдены и
исправлены в этой же правке: (1) Шаг 4/5 читались как «оба обязательны на Глубоком
разборе», реально были пропущены без критерия — теперь явная таблица выбора; (2) из 4
уникальных режимов использован только 1 (Аномалия) без объяснения — теперь минимум 2 с
критерием выбора. `confidence` в frontmatter обновлён, `status` остаётся `draft` — n=1,
рано на `dogfooded`/`wired` в терминах `skills/registry.yaml`.

**v1.0 (2026-08-04)** — первая версия, собрана после проверки что 70-80% функциональности
уже покрыто существующими скиллами (см. «Происхождение» выше).
